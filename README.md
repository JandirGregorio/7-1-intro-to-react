# 7-1-intro-to-react

To create a new Vite React project, use the command below:

```sh
npm create vite
```

You can copy the provided `images/` directory into the `public/` directory. The images inside can be referenced as relative paths like so:

```jsx
<img src="images/cat.jpg">
```